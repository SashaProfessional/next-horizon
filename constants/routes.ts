export enum Routes {
    HOME = "/",
    LOGIN = "/login",
    REGISTER = "/register",
    RESET_PASSWORD = "/reset-password",
    ACCOUNT = "/account",
    LOGOUT = "/log-out",
    DASHBOARD = "/dashboard",
  }